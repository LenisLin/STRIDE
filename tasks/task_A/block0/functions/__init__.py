"""Internal helper modules for Task A Block 0.

The public Block 0 surface lives in `tasks.task_A.block0` as execute/analyze
entrypoints. This package holds cache, metric, schema, progress, permutation,
and writer helpers used by those entrypoints.
"""
from __future__ import annotations

__all__: list[str] = []
