# Constraints (Locked)

These constraints govern STRIDE as the live project/scientific surface. The
canonical core-package direction is `src/stride/`.

C1. Repo-as-memory: locked constraints live here; undocumented constraints are not binding.
C2. Strict Configuration Decoupling: The reusable library layer (`src/stride/`) is STRICTLY FORBIDDEN from importing yaml or reading config files directly. The repository MAY provide config template files, but they exist only as task-layer instantiation references. All concrete path decisions, config reads, config interpretation, hyperparameters, and runtime configurations must be performed in `tasks/task_*/` (or equivalent task-scoped entrypoints) and then passed into the library layer as explicit function arguments.
C3. No silent failures: logging + validation + fail-fast assertions are mandatory. Programmer-level contract violations MUST fail fast via canonical `ContractError` in low-level engines; `DataContractError` is a non-canonical alias. Per-item batched data degeneracies (empty mass/support, numerical instability) MUST be isolated with explicit fit-status diagnostics and NaN metrics, then bypassed with structured audit logs at the pipeline level. Numerical padding (for example adding epsilon to empty vectors to force convergence) is strictly forbidden.
C4. No speculative science: uncertain algorithms/metrics must go through Tier-2 gate.
C5. Python→R bridge: any handover file must be created via `write_r_handover()`. Output directory, file name, primary key, and export format are task-scoped decisions and must be passed explicitly; the reusable library layer does not discover them from yaml/config.
C6. Domain-Logic Isolation (Library vs. Tasks): The physical boundary is two-tier on the live side: reusable core library (`src/stride/`) and task layer (`tasks/`). The reusable core MUST remain task-insensitive and may contain both method kernel modules and method-defining core inference modules of STRIDE itself; `tasks/` MUST own cohort-specific modeling choices, study design, inclusion/exclusion rules, config parsing, explicit cohort/patient/visit looping, endpoint/covariate/subgroup orchestration, reporting, and benchmark-specific evaluation. Hard-coded cohort names, benchmark-specific clinical semantics, task-specific formula construction, and reporting logic are forbidden in the reusable core. Benchmark code belongs under `tasks/` or `tasks/_shared/benchmarks/`.
C7. Representation Contract: The reusable core may contain official representation utilities and high-level validation for the official AnnData route. Raw single-cell or spot modalities MUST NOT enter UOT directly. Any official or custom representation route MUST terminate in canonical STRIDE-ready objects on a shared `K`-state basis, even if the implementation route uses prototypes internally. In the current first-pass official route, that shared basis MUST be built tissue-agnostically before any tissue/domain stratification: per-cell subtype labels -> within-ROI kNN neighborhood subtype proportion vectors -> k-means shared community states, with user-configurable `k` and documented first-pass default `20`. Tissue/domain labels MUST NOT be injected into community-state construction. The current first-pass ROI/FOV observation input is then a normalized community-composition vector over that basis together with separate uniform observation mass (`mass = 1`, `mass_mode = "uniform"`). A cost matrix of shape `[K, K]` on that same basis, required global scaling objects such as `s_C` where mandated by the method, and a compatible route to valid observation-layer calibration controls of shape `[N]` when task-specific calibration is required remain required. In the official route, local representation construction, shared-basis construction, and official cost geometry construction are coupled library-supported steps that MUST remain geometrically consistent; users following the official route do not manually invent `cost_matrix`. Density mode depends on ROI area metadata and MUST preserve declared physical cells/mm² semantics where relevant for future/custom routes. Custom routes may replace raw-data-to-state-space mapping, aggregation, or cost construction, but may not redefine the mathematical meaning of canonical STRIDE inputs.
C8. Task-Driven Calibration Order: The task layer defines baseline subsets, grouping context, pairing metadata, and any observation-domain stratification first. Library calibration then uses that task-compatible context to derive the required observation-layer calibration controls; after that, the task layer assembles final solver batches `[N, K]`, and the library runs observation-layer comparison on those ROI-/sample-level tensors.
C9. Baseline Comparator Requirement: Any task claiming added value from STRIDE MUST define explicit baseline comparators. At minimum, the baseline analysis MUST include a simple state-abundance comparator and MUST distinguish what the baseline already answers from what STRIDE additionally explains.
C10. Same-Scale and Task-Scoped Baseline Rule: Baseline state-abundance comparators MUST be computed on the same physical or pseudo-mass scale as the canonical mass table used by STRIDE in that task. If STRIDE uses density, the baseline MUST use that same density scale; presenting density-based STRIDE results against counts- or proportion-based baselines as if they were method-equivalent is forbidden. It is also forbidden to present simple abundance changes as if they were uniquely discoverable only by STRIDE. Baseline evaluation, reporting, and any downstream baseline tables remain task-scoped rather than core-library-level.
C11. Patient-Relation Semantics: The canonical patient object is `(T_p, e_p)` with `T_p = [A_p | d_p]`. Docs and exported semantics MUST treat `A_p` as a row-substochastic continuity/remodeling operator satisfying `sum_j A_{p,ij} + d_{p,i} = 1`. If a normalized conditional kernel is discussed, it MUST be documented as a derived auxiliary object from `A_p` and `d_p`, not as the canonical patient object itself.
C12. Burden/Composition Separation: `mu_p^-`, `mu_p^+`, `m_p^(d)`, and `m_p^(e)` live on a patient-level pseudo-mass / burden scale on the shared `K`-state basis. `q_p^-` and `q_p^+` are derived normalized compositions only. Conservation language is limited to a soft burden-consistency anchor; docs and reports MUST weaken or disable burden-level claims when coverage or platform comparability is weak.
C13. Observation-Layer Canonical Object: The canonical observation layer in the current first pass is a domain-stratified bag-of-FOV empirical measure over ROI/FOV community-composition vectors with equal ROI/FOV mass (`mass = 1`, `mass_mode = "uniform"`). OT / Sinkhorn MAY be used to compare those empirical measures, but it MUST remain an observation-layer comparison tool. Docs and reports MUST keep ROI/FOV community composition distinct from the cell-level neighborhood subtype composition used during state construction, and they MUST NOT present multinomial, Dirichlet-multinomial, logistic-normal, or dense transport-plan framing as the canonical biological ontology.
C14. No State-Domain Double Counting: Domain/compartment labels are observation-layer stratification variables, grouped discrepancy variables, bridge input grouping variables, covariates, or analysis surfaces only. The shared `K`-state basis MUST NOT encode domain identity and then condition on domain again. Domain labels MUST NOT define state geometry or the axes of `A_p`, `d_p`, and `e_p`. Task-local ordered tissue-domain analyses, including Task A, do not redefine canonical state identity.
C15. Current Uncertainty Surface: The current core-package uncertainty meaning is bootstrap/sampling-variance uncertainty over realized patient bridge outputs (`A`, `d`, `e`). The current first-pass core contract uses bootstrap/sampling-variance uncertainty terminology.

# Versioning Model (Lock one)
# - Model A: single repo SemVer
# - Model B: component SemVer
# - Model C: single repo SemVer + API_VERSION
